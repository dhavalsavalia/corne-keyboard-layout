layout:
  qmk_keyboard: corne_rotated
  qmk_layout: LAYOUT_split_3x6_3

draw_config:
  key_h: 56
  key_w: 60
  split_gap: 30
  combo_h: 26
  combo_w: 28

layers:
  # ═══════════════════════════════════════════════════════════════════════════
  # BASE: QWERTY + Homerow Mods (GACS / SCAG)
  # ═══════════════════════════════════════════════════════════════════════════
  # Outer columns: `, Caps Word, (empty) | -, ', (empty)
  # Homerow: A=Cmd, S=Alt, D=Ctrl, F=Shift | J=Shift, K=Ctrl, L=Alt, ;=Cmd
  # Thumb: ESC(Media) | SPACE(Sym) | TAB ||| RET | BSPC(Num) | DEL(Fun)
  # ═══════════════════════════════════════════════════════════════════════════
  Base:
    - ["`", Q, W, E, R, T, Y, U, I, O, P, "-"]
    - [Caps Word, {t: A, h: LGUI}, {t: S, h: LALT}, {t: D, h: LCTRL}, {t: F, h: LSHFT}, G, H, {t: J, h: RSHFT}, {t: K, h: RCTRL}, {t: L, h: RALT}, {t: ";", h: RGUI}, "'"]
    - [C-s, Z, X, C, V, B, N, M, ",", ".", "/", ""]
    - [{t: ESC, h: Media}, {t: SPACE, h: Sym}, TAB, RET, {t: BSPC, h: Num}, {t: DEL, h: Fun}]

  # ═══════════════════════════════════════════════════════════════════════════
  # NUM: Number Layer (hold BSPC)
  # ═══════════════════════════════════════════════════════════════════════════
  # Left side: Numpad-style (7-8-9 / 4-5-6 / 1-2-3) + operators
  # Right side: Modifiers for one-handed Cmd+1 style combos
  # Thumb: . | 0 | - for decimal entry
  # ═══════════════════════════════════════════════════════════════════════════
  Num:
    - ["", "[", "7", "8", "9", "]", "", "", "", "", "", ""]
    - ["", ";", "4", "5", "6", "=", "", RSHFT, RCTRL, RALT, RGUI, ""]
    - ["", "`", "1", "2", "3", "\\", "", "", "", "", "", ""]
    - [".", "0", "-", "", {type: held}, ""]

  # ═══════════════════════════════════════════════════════════════════════════
  # SYM: Symbol Layer (hold SPACE)
  # ═══════════════════════════════════════════════════════════════════════════
  # Optimized for Python/TypeScript
  # Left 3x5: comparisons, math, brackets
  # Right 3x5: brackets, punctuation, arrow macro (→/⇒)
  # ═══════════════════════════════════════════════════════════════════════════
  Sym:
    - ["", ",", "<", ">", "-", "|", "^", "{", "}", "$", "→", ""]
    - ["", "!", "*", "/", "=", "&", "#", "(", ")", ";", "\"", ""]
    - ["", "~", "+", "[", "]", "%", "@", ":", "`", ".", "", ""]
    - [{type: held}, "", "", "", "", ""]

  # ═══════════════════════════════════════════════════════════════════════════
  # FUN: Function Layer (hold DEL)
  # ═══════════════════════════════════════════════════════════════════════════
  # Left side: F-keys in numpad arrangement (F12/F7-F9, F11/F4-F6, F10/F1-F3)
  # Right side: Arrow keys + page navigation for Cmd/Ctrl+Arrow workflows
  # ═══════════════════════════════════════════════════════════════════════════
  Fun:
    - ["", F12, F7, F8, F9, PSCRN, "", "", "", "", "", ""]
    - ["", F11, F4, F5, F6, SLCK, "←", "↓", "↑", "→", "", ""]
    - ["", F10, F1, F2, F3, PAUSE, HOME, PGDN, PGUP, END, INS, ""]
    - ["", "", "", "", "", {type: held}]

  # ═══════════════════════════════════════════════════════════════════════════
  # MEDIA: Media + Bluetooth (hold ESC)
  # ═══════════════════════════════════════════════════════════════════════════
  # Left side: Modifiers (for one-handed combos) + Bluetooth profiles
  # Right side: Media controls (prev/vol/next), brightness
  # ═══════════════════════════════════════════════════════════════════════════
  Media:
    - ["", "", "", "", "", "", "", "", "", "", BRI+, ""]
    - ["", LGUI, LALT, LCTRL, LSHFT, "", "⏮", VOL-, VOL+, "⏭", BRI-, ""]
    - ["", BT0, BT1, BT2, BT3, BT_CLR, "", "", "", "", "", ""]
    - [{type: held}, "", "", "⏹", "⏯", "🔇"]

# ═════════════════════════════════════════════════════════════════════════════
# SUMMARY
# ═════════════════════════════════════════════════════════════════════════════
#
# LAYER ACCESS:
#   Base    → Default
#   Num     → Hold BSPC (right thumb middle)
#   Sym     → Hold SPACE (left thumb middle)
#   Fun     → Hold DEL (right thumb outer)
#   Media   → Hold ESC (left thumb outer)
#
# THUMB CLUSTER:
#   Left:  ESC (Media) | SPACE (Sym) | TAB
#   Right: RET         | BSPC (Num)  | DEL (Fun)
#
# HOMEROW MODS (GACS / SCAG):
#   Left:  A=⌘  S=⌥  D=⌃  F=⇧
#   Right: J=⇧  K=⌃  L=⌥  ;=⌘
#
# OUTER COLUMNS (Base layer):
#   Left:  `  |  Caps Word  |  (empty)
#   Right: -  |  '          |  (empty)
#
# SPECIAL KEYS:
#   → macro: Sym layer, outputs → (shifted: ⇒)
#   Caps Word: Base layer left outer middle
#
# EMPTY SLOTS (for future use):
#   - Base layer: left outer bottom, right outer bottom
#   - Most layers: outer columns are empty (inherit or transparent)
#
# ═════════════════════════════════════════════════════════════════════════════